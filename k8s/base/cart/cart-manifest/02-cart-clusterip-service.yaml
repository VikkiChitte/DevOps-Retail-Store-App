apiVersion: v1
kind: Service
metadata:
  name: carts
  labels:
    app.kubernetes.io/name: carts
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: carts
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
